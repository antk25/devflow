# Dev Orchestrator — Руководство пользователя

Полное руководство по использованию AI-оркестратора разработки, построенного на Claude Code.

---

## Содержание

1. [Что это такое](#что-это-такое)
2. [Быстрый старт](#быстрый-старт)
3. [Управление проектами](#управление-проектами)
4. [Команды разработки](#команды-разработки)
   - [/develop — Автономная разработка](#develop--автономная-разработка)
   - [/fix — Быстрое исправление](#fix--быстрое-исправление)
   - [/refactor — Рефакторинг](#refactor--рефакторинг)
   - [/investigate — Расследование проблемы](#investigate--расследование-проблемы)
   - [/review — Код-ревью](#review--код-ревью)
   - [/finalize — Финализация коммитов](#finalize--финализация-коммитов)
   - [/plan и /implement — Ручной режим](#plan-и-implement--ручной-режим)
5. [Пакетная очередь задач](#пакетная-очередь-задач)
6. [Агенты](#агенты)
7. [Git-стратегия](#git-стратегия)
8. [E2E тестирование](#e2e-тестирование)
9. [Интеграции](#интеграции)
10. [Конфигурация проекта](#конфигурация-проекта)
11. [Устранение неполадок](#устранение-неполадок)
12. [Справочник команд](#справочник-команд)

---

## Что это такое

Dev Orchestrator — это система оркестрации разработки, работающая нативно в Claude Code. Она координирует специализированных AI-агентов для планирования, реализации, тестирования и ревью программных задач.

**Ключевые возможности:**

- **Полностью автономный режим** — от плана до финального коммита без подтверждений
- **Мульти-репозиторий** — поддержка проектов с раздельным frontend/backend
- **E2E тестирование** — curl для API, Playwright для UI
- **Валидация архитектуры** — автоматическая проверка и исправление нарушений паттернов
- **Умная маршрутизация** — автоопределение типа задачи (фича, баг, рефакторинг)
- **Пакетная очередь** — планирование работы по нескольким проектам на день

---

## Быстрый старт

При запуске нового разговора оркестратор приветствует списком команд:

```
🎯 Dev Orchestrator

Commands:
• /develop <feature> - Autonomous development (smart routing)
• /fix <bug> - Quick bug fix (no planning)
• /refactor <scope> - Code refactoring
• /investigate <issue> - Deep problem analysis (no changes)
• /review [--pr 123] - Code review (local or PR/MR)
• /finalize [branch] - Clean up work branch into atomic commits
• /queue add|list|run - Batch task queue across projects
• /plan, /implement - Manual workflow
• /project list|switch|add - Project management
• /help - Full documentation

Ready to build!
```

### Какую команду выбрать?

| Ситуация | Команда | Почему |
|----------|---------|--------|
| Новая фича | `/develop` | Полный пайплайн: планирование, ревью, атомарные коммиты |
| Баг (причина ясна) | `/fix` | Быстро, без фазы планирования |
| Баг (причина неясна) | `/investigate` | Сначала анализ, без изменений |
| Улучшение кода | `/refactor` | Пошаговый, с сохранением поведения |
| Ревью своих изменений | `/review` | Перед коммитом |
| Ревью PR коллеги | `/review --pr 123` | Внешнее код-ревью |
| Грязная git-история | `/finalize` | Очистка перед PR |
| Пакетная работа по проектам | `/queue` | Запланировать день, выполнить пакетом |
| Полный ручной контроль | `/plan` -> `/implement` -> `/finalize` | Пошагово с одобрением |

---

## Управление проектами

Оркестратор поддерживает работу с несколькими проектами из одного места.

### Регистрация проектов

```
/project add /home/user/projects/my-app
/project add /home/user/projects/another-app
```

При добавлении автоматически определяется:
- Тип проекта (fullstack, single, typescript, php и т.д.)
- Структура репозиториев (отдельные frontend/backend или один репозиторий)
- Наличие регистрации в Serena
- Конфигурация тестирования

### Просмотр проектов

```
/project list
```

Выводит таблицу всех зарегистрированных проектов с указанием типа, пути и активного проекта.

### Переключение между проектами

```
/project switch my-app
```

При переключении происходит:
1. Обновление активного проекта в `projects.json`
2. Активация Serena-проекта (если зарегистрирован)
3. Загрузка контекста проекта из его `CLAUDE.md`
4. Доступ к Serena-воспоминаниям для этого проекта

### Информация о проекте

```
/project info            # Текущий проект
/project info my-app   # Конкретный проект
```

### Удаление из реестра

```
/project remove old-project
```

Удаляет только из реестра оркестратора, файлы проекта не трогает.

---

## Команды разработки

### /develop — Автономная разработка

Главная команда для создания новых фич. Полностью автономный пайплайн — никаких подтверждений не требуется.

```
/develop Добавить аутентификацию через JWT
/develop Создать систему уведомлений
/develop Реализовать экспорт данных в CSV
```

#### Что происходит

```
создание work-ветки → план → реализация → валидация → исправления → E2E тест → ревью → исправления → ФИНАЛИЗАЦИЯ → итог
                                                                                                          ↓
                                                                          создание чистой ветки с атомарными коммитами
```

Пошагово:

1. **Чтение конфигурации** — конвенции проекта, репозитории, тестирование
2. **Анализ стиля коммитов** — git log для определения формата
3. **Создание work-ветки** (`feature/xxx-work` для итераций)
4. **Планирование** (PM-агент) — разбивка на подзадачи
5. **Реализация каждой задачи** (Developer-агент)
6. **Валидация архитектуры** (Architecture Guardian)
7. **Автоматическое исправление** нарушений
8. **E2E тестирование** (curl для API, Playwright для UI)
9. **Автоматическое исправление** тестовых ошибок
10. **Коммит изменений** (WIP-коммиты в work-ветке)
11. **Код-ревью** (Reviewer-агент)
12. **Автоматическое исправление** критических замечаний
13. **ФИНАЛИЗАЦИЯ** — создание чистой ветки с атомарными коммитами
14. **Итоговый отчёт** с обеими ветками

#### Стратегия двух веток

```
feature/auth-work  ← резервная копия (12 грязных коммитов)
feature/auth       ← пушите эту (3 атомарных коммита)
```

- **Work-ветка** (`-work` суффикс) — все итерации, исправления, WIP. Грязная история — нормально.
- **Финальная ветка** (чистое имя) — атомарные, логичные коммиты. Готова для PR.
- Work-ветка сохраняется как резервная копия.

#### Умная маршрутизация

`/develop` автоматически определяет тип задачи по ключевым словам:

| Ключевые слова | Маршрут |
|---------------|---------|
| fix, bug, broken, error, TypeError, не работает, баг, сломан, ошибка | -> `/fix` |
| refactor, clean up, restructure, extract, рефактор, очистить | -> `/refactor` |
| Остальное | Полный пайплайн `/develop` |

Примеры:
```
/develop Fix login button       → автоматически /fix
/develop Refactor auth service  → автоматически /refactor
/develop Add user dashboard     → полный пайплайн
```

#### Мульти-репозиторий

Для проектов с раздельными frontend/backend:
- Создаёт одноимённые ветки в каждом репозитории
- Git-операции выполняются из директории каждого репо
- Коммиты создаются отдельно для каждого репо

#### Итоговый отчёт

После завершения выводится подробный отчёт:
- Ветки для каждого репозитория
- Список атомарных коммитов
- История work-ветки
- Изменённые файлы
- Результат валидации архитектуры
- Результат E2E тестирования
- Результат код-ревью
- Команды для `git push`

**Вы контролируете только финальный `git push`.**

---

### /fix — Быстрое исправление

Оптимизированный пайплайн для багов и небольших исправлений. Без фазы планирования.

```
/fix Кнопка логина не реагирует
/fix TypeError в профиле пользователя
/fix Сломанная пагинация в API
```

#### Пайплайн

```
создание ветки → поиск → реализация → тест → коммит → итог
```

#### Что происходит

1. Чтение конфигурации проекта
2. Создание fix-ветки (`fix/xxx`)
3. Поиск проблемы (нахождение затронутых файлов)
4. Анализ корневой причины
5. Реализация исправления
6. Запуск тестов
7. Коммит с префиксом `fix:`
8. Краткий итог

#### Когда использовать

- Баги с понятной причиной
- Мелкие ошибки
- Опечатки
- Сломанная функциональность

---

### /refactor — Рефакторинг

Структурированный пайплайн рефакторинга с пошаговым выполнением и сохранением поведения.

```
/refactor src/services/auth.ts       # Конкретный файл
/refactor src/components/            # Директория
/refactor Payment service            # По описанию
/refactor --extract UserValidator from UserService
```

#### Пайплайн

```
создание ветки → анализ → рефакторинг (пошагово) → валидация → тест → коммит → итог
```

#### Что происходит

1. Чтение паттернов проекта
2. Создание refactor-ветки (`refactor/xxx`)
3. Анализ текущей структуры кода
4. Планирование шагов рефакторинга
5. Реализация пошагово (коммит после каждого шага)
6. Валидация архитектуры
7. Запуск полного набора тестов
8. Итог с метриками до/после

**Ключевой принцип:** тесты должны проходить после каждого шага.

---

### /investigate — Расследование проблемы

Глубокий анализ багов и проблем **без внесения изменений в код**.

```
/investigate Логин не работает в Safari
/investigate Утечка памяти в дашборде
/investigate Почему API отвечает медленно?
```

#### Пайплайн

```
поиск → анализ → гипотезы → отчёт с решениями (БЕЗ изменений)
```

#### Что происходит

1. Разбор описания симптомов
2. Поиск свидетельств (сообщения об ошибках, связанный код)
3. Трассировка потока кода
4. Формирование гипотез (ранжированных по вероятности)
5. Предложение решений для каждой гипотезы
6. Генерация подробного отчёта

#### Результат включает

- Анализ корневой причины с доказательствами
- Несколько гипотез, ранжированных по уверенности
- Варианты решений с оценками сложности/риска
- Следующие шаги (например, команда `/fix` с конкретной целью)

#### Флаги

| Флаг | Описание |
|------|----------|
| `--deep` | Более тщательный анализ, проверка git-истории |
| `--quick` | Быстрое сканирование, только топ-3 подозреваемых |
| `--test` | Дополнительно запуск тестов для сбора данных |

---

### /review — Код-ревью

Комплексное код-ревью с поддержкой внешних PR/MR.

```
/review                            # Staged-изменения
/review src/api/                   # Конкретная директория
/review --pr 123                   # GitHub PR
/review --mr 45                    # GitLab MR
/review --branch feature/auth      # Ветка vs main
```

#### Что проверяется

- **Безопасность** — уязвимости из OWASP Top 10
- **Производительность** — N+1 запросы, утечки памяти
- **Качество кода** — лучшие практики
- **Покрытие тестами**

#### Ревью внешних PR/MR

Для PR коллег или open-source:
- Получение diff и метаданных из GitHub/GitLab
- Сбор контекста о кодовой базе
- Ревью с указанием авторства
- Возможность публикации комментариев обратно в PR (флаг `--comment`)

#### Флаги

| Флаг | Описание |
|------|----------|
| `--focus security` | Только проблемы безопасности |
| `--quick` | Быстрое ревью, только критические замечания |
| `--comment` | Опубликовать комментарии в PR/MR |
| `--base develop` | Сравнение не с main, а с указанной веткой |

---

### /finalize — Финализация коммитов

Создание чистой ветки с атомарными коммитами из грязной work-ветки.

```
/finalize                      # Текущая ветка
/finalize feature/auth-work    # Конкретная ветка
```

#### Пайплайн

```
анализ work-ветки → группировка изменений → создание чистой ветки → атомарные коммиты → проверка
```

#### Когда использовать

- Ручная разработка оставила грязную историю
- `/develop` был прерван до финализации
- Любая ветка нуждается в очистке перед PR

#### Как группируются коммиты

| Группа | Тип коммита |
|--------|-------------|
| Новые модели/сущности | `feat:` |
| Новые API-эндпоинты | `feat:` |
| Новые UI-компоненты | `feat:` |
| Рефакторинг | `refactor:` |
| Тесты | `test:` |
| Конфигурация | `chore:` |

Формат коммитов анализируется из git-истории проекта и копируется.

---

### /plan и /implement — Ручной режим

Для пошаговой работы с контролем на каждом этапе.

#### Планирование

```
/plan Добавить авторизацию через JWT
```

PM-агент:
1. Анализирует требования
2. Разбивает на подзадачи
3. Оценивает сложность
4. Определяет зависимости

#### Реализация

```
/implement Добавить авторизацию через JWT
/implement 1    # Реализовать задачу #1 из плана
```

Автоматически выбирает подходящего Developer-агента по:
- Расширениям файлов (.ts, .tsx -> JS Developer)
- Обнаружению фреймворка (Laravel -> PHP Developer)
- Явному указанию

#### Типичный ручной workflow

```
/plan Add login feature      # Создать план
/implement 1                 # Реализовать задачу #1
/implement 2                 # Реализовать задачу #2
/review                      # Ревью изменений
/finalize                    # Очистка коммитов перед пушем
```

---

## Пакетная очередь задач

Команда `/queue` позволяет запланировать работу на день по нескольким проектам и выполнить задачи пакетом. Каждый элемент вызывает существующий скилл (`/develop`, `/fix`, `/refactor` и т.д.) с автоматическим переключением проекта.

### Добавление задач

```
/queue add my-app: develop Добавить тёмную тему
/queue add my-app: fix Исправить валидацию формы логина
/queue add my-api: refactor Очистка auth-сервиса
/queue add my-api: develop Добавить экспорт данных
/queue add develop Реализовать поиск          # активный проект
```

**Формат:** `/queue add <проект>: <скилл> <описание>` или `/queue add <скилл> <описание>` (для активного проекта).

**Допустимые скиллы:** `develop`, `fix`, `refactor`, `investigate`, `review`, `plan`, `implement`.

### Просмотр очереди

```
/queue list              # Только ожидающие
/queue list --all        # Все, включая выполненные и ошибочные
```

Выводит таблицу задач, сгруппированную по проектам, с указанием статуса.

### Выполнение очереди

```
/queue run               # Все ожидающие задачи
/queue run 3             # Начать с задачи #3
```

#### Порядок выполнения

1. Сброс зависших элементов (`running` -> `pending`)
2. Фильтрация ожидающих задач по порядку ID
3. Сохранение текущего активного проекта
4. Для каждой задачи:
   - Переключение проекта (если нужно) — обновление `projects.json` и Serena
   - Обновление статуса на `running`
   - Вызов скилла через инструмент `Skill`
   - Фиксация результата (`completed` или `failed`)
5. Восстановление исходного активного проекта
6. Обновление блока `last_run`
7. Итоговый отчёт

**Ошибочные задачи не блокируют очередь** — выполнение продолжается.

### Удаление задач

```
/queue remove 2           # Удалить задачу #2
/queue remove 2 3 5       # Удалить несколько
```

### Очистка очереди

```
/queue clear              # Только ожидающие
/queue clear --all        # Всё
```

### Статус последнего запуска

```
/queue status
```

Показывает:
- Время и результаты последнего запуска
- Текущее состояние очереди по статусам
- Детали ошибочных задач

### Особые случаи

| Случай | Поведение |
|--------|-----------|
| Несуществующий проект | Ошибка + список доступных проектов |
| Несуществующий скилл | Ошибка + список допустимых скиллов |
| Пустая очередь при `run` | Сообщение, выход |
| Ошибка в задаче при `run` | Отметка `failed`, переход к следующей |
| Прерванный запуск | Задачи со статусом `running` сбрасываются в `pending` при следующем `run` |
| Отсутствие `queue.json` | Создаётся пустой файл при первом `add` или `list` |
| Дубликаты задач | Допускаются — можно удалить через `list` + `remove` |

---

## Агенты

Система использует специализированных агентов для разных фаз разработки:

| Агент | Роль | Применение |
|-------|------|------------|
| **PM** (Project Manager) | Менеджер проекта | Анализ требований, декомпозиция задач, планирование |
| **Architect** | Системный архитектор | Проектирование архитектуры, ADR, технические решения |
| **JS Developer** | JavaScript/TypeScript разработчик | React, Vue, Node.js, TypeScript |
| **PHP Developer** | PHP разработчик | Laravel, Symfony, PHP |
| **Tester** | QA-инженер | Модульные, интеграционные, E2E тесты |
| **Reviewer** (Code Reviewer) | Код-ревьюер | Аудит безопасности, производительность, качество кода |
| **Architecture Guardian** | Валидатор паттернов | Проверка кода на соответствие паттернам проекта |

### Выбор агента

Агент выбирается автоматически в зависимости от:
- **Расширения файлов** — `.ts`, `.tsx` -> JS Developer; `.php` -> PHP Developer
- **Обнаружения фреймворка** — наличие `package.json`, `composer.json` и т.д.
- **Типа задачи** — планирование -> PM, ревью -> Reviewer, валидация -> Architecture Guardian

---

## Git-стратегия

### Автоматическое управление ветками

Каждая команда имеет свою стратегию работы с git:

| Команда | Work-ветка | Финальная ветка | Стиль коммитов |
|---------|------------|-----------------|----------------|
| `/develop` | `feature/xxx-work` | `feature/xxx` | Атомарные (по стилю проекта) |
| `/fix` | — | `fix/xxx` | Один коммит |
| `/refactor` | — | `refactor/xxx` | По шагам |
| `/investigate` | — | — | Без изменений |
| `/review` | — | — | Без изменений |
| `/queue` | (по элементу) | (по элементу) | Делегируется вызванному скиллу |

### Двухветочная стратегия (/develop)

```
main ─────────────────────────────────────────────
    \                                            \
     feature/auth-work                            feature/auth
     (все итерации,                               (3 атомарных коммита,
      15 грязных коммитов)                         готова для PR)
```

- **Work-ветка** — все итерации, исправления, рефакторинг. Грязная история — нормально.
- **Финальная ветка** — чистые, атомарные, логичные коммиты.
- Work-ветка сохраняется как резервная копия.

### Стиль коммитов

Формат коммитов анализируется из git-истории проекта:
- **Conventional Commits:** `feat(scope): description`
- **Простой:** `Add feature description`
- **С тикетом:** `[PROJ-123] Description`

### Безопасность

- `git push` **всегда вручную** — заблокирован в настройках
- `gh` (GitHub CLI) **всегда вручную**
- Все изменения остаются локальными до вашего пуша
- Полный итоговый отчёт для ревью перед пушем

---

## E2E тестирование

После реализации оркестратор автоматически проверяет работоспособность фичи.

### Backend (API)

- Использует `curl` для тестирования затронутых эндпоинтов
- Валидирует статус и структуру ответа
- Если сервер не запущен — пропускает с пометкой

```bash
curl -X POST http://localhost:8000/api/endpoint \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}' | jq .
```

### Frontend (UI)

- Использует Playwright MCP или `npx playwright test`
- Тестирует затронутые пользовательские сценарии
- Если frontend не запущен — пропускает с пометкой

### Результат

```
E2E Test Results
- Backend API: ✅ Passed (или ⏭️ Skipped - server not running)
- Frontend UI: ✅ Passed (или ⏭️ Skipped - not running)
```

---

## Интеграции

### Serena MCP

Проекты, зарегистрированные в Serena (через `.serena/config.yaml`), получают:
- **Символьная навигация** — поиск символов, ссылок, определений
- **Персистентная память** — контекст сохраняется между сессиями
- **Умный поиск кода** — language-aware сопоставление паттернов

Автоматически активируется при `/project switch`.

### Локальная RAG база знаний

Оркестратор использует локальную RAG (mcp-local-rag) для хранения и извлечения документации, архитектурных паттернов и справочных материалов.

**Расположение:** `~/projects/rag-knowledge/`

```
~/projects/rag-knowledge/
├── shared/              # Кросс-проектные знания
│   ├── patterns/        # Паттерны проектирования, SOLID, DDD
│   ├── architecture/    # Архитектурные подходы, примеры ADR
│   └── languages/       # Справочники по языкам/фреймворкам
├── my-app/              # Документация проекта
├── my-api/              # Документация проекта
└── claude-orchestrator/ # Документация проекта
```

**Как работает интеграция:**

1. Оркестратор читает имя активного проекта из `projects.json`
2. Запрашивает RAG с именем проекта + ключевые слова фичи
3. Фильтрует результаты по релевантности (только score < 0.45)
4. Внедряет как контекстную секцию в промпты агентов (макс. ~2000 символов)
5. Агенты используют этот контекст наряду с анализом кода

**Если RAG недоступен** — пропускается без ошибки, workflow продолжается.

### Другие интеграции

- **Git** — управление версиями (автоветки, автокоммиты)
- **npm / composer** — зависимости
- **Jest / PHPUnit** — тестирование
- **ESLint / PHPStan** — статический анализ
- **Playwright** — браузерное тестирование

---

## Конфигурация проекта

### Структура данных

Данные хранятся в `.claude/data/`:

| Файл | Назначение |
|------|------------|
| `projects.json` | Реестр проектов и активный проект |
| `tasks.json` | Текущие задачи и их статус |
| `queue.json` | Пакетная очередь задач и история выполнения |

### Формат проекта (v2.0)

```json
{
  "name": "my-fullstack-app",
  "path": "/home/user/projects/my-app",
  "type": "fullstack",
  "repositories": {
    "backend": "/home/user/projects/my-app/backend",
    "frontend": "/home/user/projects/my-app/frontend"
  },
  "testing": {
    "backend": {
      "type": "api",
      "base_url": "http://localhost:8000",
      "commands": {
        "unit": "cd {{repo}} && ./vendor/bin/phpunit",
        "e2e": "curl -s {{base_url}}/api/health | jq ."
      }
    },
    "frontend": {
      "type": "browser",
      "base_url": "http://localhost:3000",
      "commands": {
        "unit": "cd {{repo}} && npm test",
        "e2e": "cd {{repo}} && npx playwright test"
      }
    }
  }
}
```

**Ключевые поля:**
- `repositories` — пути к каждому git-репозиторию (обязательно для мульти-репо)
- `testing` — конфигурация E2E тестирования по компонентам
- `branch_prefix` — префикс для веток фич (например, `DEV-`)
- `serena_project` — имя проекта в Serena (если зарегистрирован)

### Паттерны проекта

Определяются в `.claude/patterns.md` в корне проекта. Оркестратор читает их при валидации кода.

Если `patterns.md` отсутствует, оркестратор:
1. Читает `CLAUDE.md` и `CONTRIBUTING.md`
2. Анализирует существующий код на паттерны
3. Проверяет git-историю на конвенции коммитов/веток

---

## Устранение неполадок

### Всё ещё запрашивает подтверждения?

**1. Перезапустите сессию Claude Code** — изменения настроек требуют перезапуска.

**2. Запустите с флагом:**
```bash
claude --dangerously-skip-permissions
```
Обходит ВСЕ проверки разрешений. Используйте осторожно.

**3. Проверьте auto-approve хук:**
```bash
chmod +x .claude/hooks/auto-approve.sh
```

**4. Пути проектов вне оркестратора:**
Разрешения для путей за пределами директории оркестратора должны быть явно указаны в `settings.json`.

### Git-операции не работают

Git-команды **должны всегда запускаться из директории репозитория**, а не из директории оркестратора:

```bash
# Правильно
cd /path/to/repo && git status

# Неправильно (из директории оркестратора)
git status
```

### E2E тесты не проходят

1. Оркестратор фиксирует ошибку
2. Пытается исправить через Developer-агент
3. Перезапускает тест
4. Если после 2 попыток не получается:
   - Сообщает об ошибке с деталями
   - Продолжает с ревью (код может быть корректен, проблема в окружении)

---

## Справочник команд

### Основные команды

| Команда | Описание | Изменяет код |
|---------|----------|:------------:|
| `/develop <фича>` | Автономная разработка с полным пайплайном | Да |
| `/fix <баг>` | Быстрое исправление бага | Да |
| `/refactor <область>` | Пошаговый рефакторинг | Да |
| `/investigate <проблема>` | Глубокий анализ без изменений | Нет |
| `/review [опции]` | Код-ревью (локально или PR/MR) | Нет |
| `/finalize [ветка]` | Очистка коммитов в атомарные | Да (git) |
| `/plan <фича>` | Планирование (ручной режим) | Нет |
| `/implement <задача>` | Реализация (ручной режим) | Да |

### Очередь задач

| Команда | Описание |
|---------|----------|
| `/queue add <проект>: <скилл> <описание>` | Добавить задачу с указанием проекта |
| `/queue add <скилл> <описание>` | Добавить задачу для активного проекта |
| `/queue list [--all]` | Показать очередь |
| `/queue run [id]` | Выполнить ожидающие задачи |
| `/queue remove <id> [id2...]` | Удалить задачи по ID |
| `/queue clear [--all]` | Очистить очередь |
| `/queue status` | Статус последнего запуска |

### Управление проектами

| Команда | Описание |
|---------|----------|
| `/project list` | Список проектов |
| `/project switch <имя>` | Переключиться на проект |
| `/project add <путь> [имя]` | Зарегистрировать проект |
| `/project info [имя]` | Информация о проекте |
| `/project remove <имя>` | Удалить из реестра |

### Утилиты

| Команда | Описание |
|---------|----------|
| `/help` | Показать справку |

---

## Ежедневный рабочий процесс — пример

```bash
# 1. Утро — планирование дня
/queue add my-app: develop Добавить тёмную тему
/queue add my-app: fix Исправить валидацию email
/queue add my-api: refactor Очистка AuthService
/queue add my-api: develop Добавить экспорт в Excel

# 2. Проверка плана
/queue list

# 3. Запуск — уходим за кофе
/queue run

# 4. Проверка результатов
/queue status

# 5. Ревью и пуш по каждому проекту
/project switch my-app
git log --oneline feature/dark-theme
git diff main
git push -u origin feature/dark-theme

/project switch my-api
git log --oneline feature/excel-export
git diff main
git push -u origin feature/excel-export
```

---

*Документация актуальна на январь 2026 года.*
